[project]
name = "myagent.v1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "docker>=7.1.0",
]

[dependency-groups]
dev = [
    "ollama>=0.6.1",
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
]
